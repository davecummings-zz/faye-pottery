// Announcement Bar Configuration
// Set ENABLED to true to show the announcement bar
// When ENABLED is true, the navigation will NOT be sticky
// When ENABLED is false, the navigation will be sticky

export const ANNOUNCEMENT = {
  ENABLED: true,
  TEXT: "Next Sale: Sunday, July 12th @ 10am CST",
}
